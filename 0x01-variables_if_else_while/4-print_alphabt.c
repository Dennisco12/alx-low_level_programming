#include <stdlib.h>
#include <stdio.h>
/**
 * This is where the headers end
 * return: 0
 */

int main(void);
	/**
	 * main - entry point
	 */

{
	int d = 'a';
	for (d = 0; d < 26; && d != 4; d != 16; d++)
	putchar ('a' + d);

	return (0);
}
