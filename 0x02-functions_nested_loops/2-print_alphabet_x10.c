#include "main.h"

/**
 * main - all entry point
 * description - prints alphabets in lower case 10 times
 *
 * return always 0
 */

void print_alphabet_x10(void)

{
	int d, e;
	int f;

	f = 'a';
	d == '0';
	e == '0';
while (d < 11)
	_putchar('\n');
	d++;
while (e <= 1)
	for (f = 0; f < 26; f++)
	_putchar ( 'a' + f);
	e++;
}
