#include "main.h"

/**
 * _strcpy - function that converts a string into an integer
 * @dest: char
 * @src: char
 * Return: char
 */

int _atoi(char *s)
{
	int i;

	i = 0;
	while (src[i])
	{
		dest[i] = src[i];
		i++;
	}
	dest[i] = '\0';
	return (dest);
}
